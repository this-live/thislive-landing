## 2026-09-12 — Landing: pillar claims match measured state (U91)

- Problem: several pillar-page claims had drifted from measured reality. Cortex cited a stale contract-test count; Mnemos cited stale, inconsistent memory-count figures across three pages; Maestro and the homepage described Vega as a "learned router" when its training signal is still maturing; Agent Fabric's execution page described loopback-only safety as the current security model when a fail-closed API-key requirement is now live.
- Change: `cortex/index.html` — contract-test count corrected `369` → `572` (both occurrences), suite version `0.4.0` → `0.4.1`. Source: U81 pillar brief (`contracts suite 572/0`, contracts 0.4.1).
- Change: `mnemos/index.html`, `index.html` — memory-count claims (`107k+`, `129k+`, `75k+`, mutually inconsistent across pages) replaced with the measured figures: `44.3k live memories`, `130.9k rows of history` (includes tombstoned history), `147 containers`. Source: U83 pillar brief (live `/health`: `live_memories 44332`, `memories {live 44332, tombstoned 86534, total 130866}`; cortex drift-detector measurement: `total_memories=130,864`, `total_containers=147`).
- Change: `mnemos/index.html` — backup-status line updated from "shipped without a vector snapshot for 40 nights... first unattended run is the next night" to reflect the now-verified state: atomic build, checksum + Qdrant-snapshot reopen, weekly restore drill in cron, restore proven live on 2026-09-12. Source: U80 pillar brief (backup v2 live, restore drill proven: restored points 44,267 == manifest, 130,783 rows == manifest, 147 container tags == manifest).
- Change: `maestro/index.html` (3 occurrences), `index.html` (1 occurrence) — "learned router" softened to "a router with benchmark priors" / "grounds Vega's priors". Vega's auto-labeling pipeline is wired onto live calls but its training signal is explicitly still maturing (maestro/index.html's own ledger already said so); routing decisions with durable receipts and the live seat board remain accurate and are unchanged. Source: U78/U84 pillar briefs (receipts store live, price book loaded, seat board live at `/v1/seats`, quota windows populated — none of these claim a trained/learned router).
- Change: `fabric/index.html` — the single-box security description updated from "bound to loopback, safe by construction... [future] mint AF_API_KEY" to reflect that fail-closed auth is live today: mutating calls require a real `AF_API_KEY` even from a local caller, with no implicit loopback trust. Source: U82b pillar brief ("THE FAIL-CLOSED AF RELEASE IS LIVE": `loopback_trust false`, tokenless mutating routes now 401).
- Not changed: Forge's homepage line ("adapter factory... no adapters trained yet") was checked against U81/forge pillar brief and is still accurate — left as-is. Agent Fabric's "budget hard rule" and "leases" language (U48/U52) was checked and no such literal claim exists on the current fabric page to correct.
- Reason: Cortex Suite program unit U91 — landing PR bodies from U10 flagged the Cortex and Mnemos figures as stale; this unit re-grounds every pillar page's numeric and superlative claims in the 2026-09-12 pillar-brief measurements per program plan `2026-09-12-PROGRAM-PLAN.md`.
- Verified: local `python3 -m http.server`, curled every edited page — `/`, `/cortex/`, `/mnemos/`, `/maestro/`, `/fabric/`, `/forge/` all return 200; new figures (`572 contract tests`, `44.3k live memories`, `130.9k rows`, `147 containers`) present on the expected pages; zero remaining occurrences of `369 contract`, `107k+`, `129k+`, `75k+`, or `learned router` anywhere in the repo's HTML.
- Rollback: revert this PR's commit. No structural changes — text-only corrections within existing markup; page layout and voice unchanged.
